/*
 * AI 脚本助手生成结果示例
 *
 * 用途:
 * - 作为 AI 生成脚本的安全起点。
 * - 展示生成后应由用户预览、应用、保存, 再手动点击运行。
 *
 * 权限:
 * - 无。
 *
 * 预期输出:
 * - 控制台打印设备宽高、当前时间和一条提示。
 *
 * 安全边界:
 * - 本示例不请求截图、无障碍、悬浮窗、Root、Shizuku 或 Shell。
 * - AI 生成包含高风险 API 的脚本时, 应先阅读预览中的风险提示。
 */

'use strict';

console.show();
console.log('AI generated script preview example');
console.log('Screen:', device.width + 'x' + device.height);
console.log('Time:', new Date().toLocaleString());
toast('AI 脚本助手示例已运行');
