# A1-A16 总体验收与发布闭环

> 日期: 2026-06-10
> 对应任务: T67 / 底层能力 A1-A16 总体验收与发布闭环
> 范围: T51-T66 任务完成状态、第一阶段建议验收清单、脚本 API 文档/示例同步、构建验证、发布剩余项

## 结论

A1-A16 的实现任务已经分别落入 T51-T66, 各任务均有执行记录、验收记录、文档/示例同步说明和设备不可用限制说明。T67 本轮重点是做总体验收和发布闭环记录。

当前发布闭环仍有两个外部条件未满足:

- 当前没有连接 Android 设备, 因此截图长跑、inrt 首启、插件安装、远程调试和 UI 端到端路径只能记录设备阻塞, 不能声称已完成真机验收。
- AutoJs6 与 AutoJs6-Documentation 两个工作树包含 T50-T66 的大量未提交改动和新增文件。用户未要求提交, 本轮不擅自创建提交; 因此“工作树干净、阶段提交清晰”属于发布前剩余项。

## A1-A16 覆盖矩阵

| 能力 | 对应任务 | 状态 | 主要交付 |
| --- | --- | --- | --- |
| A1 统一能力状态中心与运行前预检 | T52 | 完成 | `capabilities` 运行时模块、能力注册表、运行前预检、AI/编辑器/打包复用 |
| A2 API/文档/补全/AI 索引一致性 | T51 | 完成 | 修复内置索引漂移、AI 能力索引一致性测试、外部文档索引同步 |
| A3 可靠自动化执行 DSL | T56 | 完成 | `auto.waitUntil`、`auto.retry`、`auto.stableClick`、`auto.stableSetText`、`auto.findWithScroll` |
| A4 UI 树快照、差异与选择器评分 | T55 | 完成 | `auto.snapshot`、`auto.diffSnapshot`、选择器评分、诊断示例 |
| A5 屏幕感知管线 | T60 | 完成 | `vision.targets`、`vision.findText`、`vision.findButton`、`vision.observe`、`vision.waitForScene` |
| A6 截图会话管理和性能基准 | T57 | 完成 | `images.openCaptureSession`、帧缓存、错误分类、性能指标和预设 |
| A7 Shizuku/Root 结构化特权 API | T59 | 完成 | `shizuku.app/settings/package/input/process/users/audit`、参数校验和审计 |
| A8 任务调度可靠性与运行记录 | T54 | 完成 | 定时任务运行记录、失败原因、调度策略和可靠性记录 |
| A9 脚本能力清单和安全沙箱 | T61 | 完成 | `project.json` 能力清单、风险策略、授权记忆、审计日志 |
| A10 插件 SDK、权限声明与供应链审计 | T62 | 完成 | 插件 manifest、索引安全字段、sha256/签名校验、插件诊断 |
| A11 远程调试、DevTools 与日志观测 | T63 | 完成 | 运行观测模型、远程调试桥、运行详情、访问 token |
| A12 AI 自动化 Copilot 二期 | T58 | 完成 | 能力图谱接入、补丁应用、隐私策略、生成到修复闭环 |
| A13 数据、网络、IPC 能力增强 | T64 | 完成 | 命名 HTTP client、断点下载、KV namespace、本地 IPC、typed Broadcast |
| A14 打包/inrt 运行能力扩展 | T65 | 完成 | 打包预检、`build-diagnostics.json`、inrt 首启/失败诊断、AI 项目清单 |
| A15 设备兼容实验室与基准测试基础 | T53 | 完成 | 设备兼容 smoke 基础、调度策略基准和设备矩阵记录 |
| A16 模板、示例和自动化方案库 | T66 | 完成 | 8 类自动化方案库、内置方案文档、AI 优先检索、外部文档同步 |

## 第一阶段建议验收清单

| 验收项 | 覆盖结果 |
| --- | --- |
| 运行时核心 API、内置文档、补全索引、AI 索引不再互相漂移 | T51 建立索引一致性校验; T52-T66 每个新增 API 均同步 `indices/all.json`、AI 索引和内置/外部文档; T67 继续执行 JSON 和索引相关单测。 |
| 用户运行脚本前能看到缺失能力和可跳转的授权入口 | T52/T61/T65 接入 `capabilities.check/ensure/explain/manifest`、编辑器运行前预检、打包预检和 inrt 首启提示。 |
| 定时任务失败后能看到真实运行记录和失败原因 | T54 新增定时任务运行记录、失败原因、运行历史数据库和调度后端记录。 |
| AI 生成脚本会基于真实能力图谱声明权限和风险 | T51/T52/T58/T59/T66 将 `AiCapabilityIndex`、`CapabilityRegistry`、Copilot 预检和方案库串联, 未知 API 和未声明高风险能力会阻断或提示。 |
| 复杂 UI 自动化示例不再依赖大量裸 `sleep` | T56/T66 示例优先使用可靠 DSL, 方案库覆盖启动等待、列表滚动、表单填写、OCR 点击等场景。 |
| 失败诊断包至少包含日志、异常、能力状态、UI 快照、可选截图 | T55/T57/T63/T65 组合提供 UI 快照、截图 Session 指标、运行观测和 inrt/build 诊断 JSON。 |

## 脚本 API 同步闭环

本轮核对的新增/修改脚本 API 已覆盖以下同步面:

- 运行时注入: `ScriptRuntime.kt` 或对应运行时宿主模块。
- API 实现: `runtime/api` 与 `runtime/api/augment`。
- 能力治理: `CapabilityRegistry` / `ProjectCapabilitySecurity` / AI 能力索引。
- 内置文档和索引: `app/src/main/assets-app/docs/*.html`, `docs/sidebar.html`, `docs/all.html`, `indices/all.json`。
- 内置示例: `app/src/main/assets-app/sample/**` 和 `app/src/main/assets-app/solutions/automation-solutions.json`。
- 外部文档: `/Users/blaze/work/github/AutoJs6-Documentation/api/*.md` 与 `json/runtimeApiIndexData.json`。

## 本轮验证结果

T67 本轮已执行:

- `jq empty app/src/main/assets-app/indices/all.json`: 通过。
- `jq empty app/src/main/assets-app/solutions/automation-solutions.json`: 通过。
- `jq empty /Users/blaze/work/github/AutoJs6-Documentation/json/runtimeApiIndexData.json`: 通过。
- `./gradlew --no-daemon -Dkotlin.incremental=false :app:testAppDebugUnitTest`: 通过。
- `./gradlew --no-daemon -Dkotlin.incremental=false :app:compileAppDebugKotlin :app:compileAppDebugJavaWithJavac`: 通过。
- `./gradlew --no-daemon -Dkotlin.incremental=false :app:compileAppDebugAndroidTestKotlin`: 通过。
- `./gradlew --no-daemon -Dkotlin.incremental=false :app:assembleAppDebug`: 通过; 命令自动推进 `version.properties` 构建号/时间戳, 已作为验证副作用还原。
- `git diff --check`: 通过。
- `git -C /Users/blaze/work/github/AutoJs6-Documentation diff --check`: 通过。
- `/Users/blaze/Library/Android/sdk/platform-tools/adb devices`: 无连接设备, 仅输出设备列表表头。

## 发布剩余项

- 连接 Android 设备后补跑真机验收: 截图 10 分钟长跑、方向切换恢复、inrt 首启授权引导、插件安装/启用、远程调试桥、内置文档/示例 UI 路径。
- 用户确认提交策略后, 将 T50-T67 主仓与外部文档仓库改动拆成阶段提交, 直到两个工作树干净。
