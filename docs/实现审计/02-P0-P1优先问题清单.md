# P0/P1 优先问题清单

## P0 立即处理

| ID | 文件 | 问题 | 原因/风险 | 解决办法 | 严重程度 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- |
| P0-01 | `app/src/main/AndroidManifest.xml:551`, `app/src/main/java/org/autojs/autojs/external/open/RunIntentActivity.java:39`, `app/src/main/java/org/autojs/autojs/external/ScriptIntents.kt:35` | 导出的 `RunIntentActivity` 可直接执行外部传入脚本 | 任意应用可传 `content://`、`path` 或 `script` 让 AutoJs6 以自身权限执行 JS, 可组合 `shell/files/shizuku/accessibility` | 外部入口改为预览/确认; 内部执行入口加签名权限或设为非导出; 禁止外部 caller 直接传 `EXTRA_KEY_PRE_EXECUTE_SCRIPT` | `CRITICAL` | `P0` |
| P0-02 | `app/src/main/AndroidManifest.xml:519`, `app/src/main/java/org/autojs/autojs/external/tasker/FireSettingReceiver.java:28` | Tasker receiver 导出且无权限约束 | JSON 只校验 `path/script`, 任意广播可转发到执行链路 | 增加可信 host/签名权限/nonce; 或接收后进入用户确认页 | `CRITICAL` | `P0` |
| P0-03 | `app/src/main/java/org/autojs/autojs/core/web/InjectableWebClient.java:51`, `app/src/main/java/org/autojs/autojs/core/web/InjectableWebClient.java:54`, `app/src/main/java/org/autojs/autojs/core/web/InjectableWebClient.java:83` | WebView 注入 `rhino.eval()` 且允许 file URL universal access | 加载不可信或可被影响的 WebView 内容时, 页面 JS 可调用 Rhino 执行宿主脚本能力 | 默认禁用 universal file access; bridge 只允许可信本地 origin; `eval` 改为受限 API; 对远程/外部 URL 禁止注入 | `CRITICAL` | `P0` |
| P0-04 | `sign.properties:3`, `sign.properties:4` | 本地发布签名口令明文存在于工作树 | 虽被 `.gitignore` 忽略, 但当前机器/备份/日志/误上传会暴露 release keystore 密码 | 立即轮换 keystore/密码; 用环境变量或 OS keychain; 保留 `sign.properties.example` 不放真实口令 | `HIGH` | `P0` |
| P0-05 | `libs/rapidocr/src/main/cpp/src/OcrUtils.cpp:380`, `libs/rapidocr/src/main/cpp/src/DbNet.cpp:34`, `libs/rapidocr/src/main/cpp/src/AngleNet.cpp:34`, `libs/rapidocr/src/main/cpp/src/CrnnNet.cpp:60` | RapidOCR asset/model 读取失败后仍构造 ORT session | `getModelDataFromAssets()` 可能返回空指针/0 长度, native 初始化可能崩溃 | 读取函数返回明确状态; 失败时抛 Java 异常并停止初始化 | `HIGH` | `P0` |
| P0-06 | `libs/rapidocr/src/main/cpp/src/main.cpp:6`, `libs/rapidocr/src/main/cpp/src/main.cpp:26`, `libs/rapidocr/src/main/cpp/src/main.cpp:62` | RapidOCR 全局 `OcrLite*` 无并发保护 | 多线程 `init/detect/benchmark` 共享 session 和成员状态, 存在竞态 | 改实例句柄 `jlong`; 或全局互斥并禁止 init/detect/release 并发 | `HIGH` | `P0` |
| P0-07 | `plugin-api/paddle-ocr-engine/src/main/java/com/baidu/paddle/lite/ocr/OCRPredictorNative.java:22` | Paddle OCR native library 加载失败后污染已加载状态 | `isSOLoaded` 在加载成功前置 true, 首次失败后后续跳过 `System.loadLibrary` | 成功后再置 true; catch 中回滚状态 | `HIGH` | `P0` |
| P0-08 | `plugin-api/paddle-ocr-engine/src/main/java/com/baidu/paddle/lite/ocr/OCRPredictorNative.java:49`, `plugin-api/paddle-ocr-engine/src/main/java/com/baidu/paddle/lite/ocr/OCRPredictorNative.java:70` | Paddle `destroy()` 未与 `runImage()` 共用锁 | native pointer 可能在 forward 过程中被 release, 造成 use-after-free | `destroy()` 加同一把锁; 或引入状态机/引用计数 | `HIGH` | `P0` |
| P0-09 | `modules/apk-parser/src/main/java/net/dongliu/apk/parser/ApkFile.java:78`, `modules/apk-parser/src/main/java/net/dongliu/apk/parser/ApkFile.java:119` | APK parser 文件句柄/ZipFile 泄漏 | `FileInputStream/FileChannel/ZipFile` 关闭路径不完整, 频繁解析 APK 会耗尽 fd | try-with-resources 关闭 channel; `close()` 中关闭 `zf` | `HIGH` | `P0` |

## P1 近期修复

| ID | 文件 | 问题 | 原因/风险 | 解决办法 | 严重程度 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- |
| P1-01 | `app/src/main/java/org/autojs/autojs/runtime/api/ProcessShell.java:206` | `shell()` 中断被包装成普通错误 | `waitFor()` 被中断后落入通用 catch, 停止脚本时 shell 子进程可能继续 | 单独捕获 `InterruptedException`, restore interrupt, destroy process, 抛 `ScriptInterruptedException` | `HIGH` | `P1` |
| P1-02 | `app/src/main/java/org/autojs/autojs/engine/ScriptEngineManager.java:106`, `app/src/main/java/org/autojs/autojs/engine/RhinoJavaScriptEngine.kt:96`, `app/src/main/java/org/autojs/autojs/engine/RootAutomatorEngine.kt:100` | engine 初始化前已注册到 manager | `stopAll()` 可能调用未初始化 thread/mThread 的 `forceStop()` | 初始化后注册; 或 forceStop 对未初始化状态 null-safe | `HIGH` | `P1` |
| P1-03 | `app/src/main/java/org/autojs/autojs/core/accessibility/SimpleActionAutomator.kt:246` | Accessibility 截屏失败后 promise 状态不清 | 失败路径未清 `mPromiseAdapter`, 后续调用可能复用已完成 promise 或挂起 | 所有成功/失败/空结果路径用 `finally` 清空并保证 resolve/reject | `HIGH` | `P1` |
| P1-04 | `app/src/main/java/org/autojs/autojs/core/accessibility/SimpleActionAutomator.kt:250` | Accessibility `ScreenshotResult.hardwareBuffer` 释放不完整 | 只回收 Bitmap, 未显式关闭原始 `HardwareBuffer`, 连续截屏可能泄漏 native buffer | copy 后 `try/finally` 关闭 hardwareBuffer 并 recycle bitmap | `HIGH` | `P1` |
| P1-05 | `app/src/main/java/org/autojs/autojs/core/image/capture/ScreenCaptureRequester.java:113` | MediaProjection FGS 启动/绑定缺异常兜底 | Android 14+ 前台服务/后台限制异常会直接崩溃 | 包裹 `startService/bindService`, 异常回传 `onRequestError()` 并 `recycle()` | `HIGH` | `P1` |
| P1-06 | `app/src/main/java/org/autojs/autojs/runtime/api/Plugins.kt:64` | 插件服务绑定后脚本退出未解绑 | 使用 application context bindService, runtime exit 未释放插件连接 | 增加 `Plugins.recycle()/close()`, 在 `ScriptRuntime.onExit()` 调用 | `HIGH` | `P1` |
| P1-07 | `build-logic/convention/src/main/kotlin/org/autojs/build/LibDeployer.kt:83`, `libs/rapidocr/build.gradle.kts:44` | 远程 native 包下载无可信校验 | 下载后生成 MD5 只能校验本地缓存, 不能防止上游替换/MITM/缓存污染 | 为每个远程包固定 SHA-256; 校验通过后再解压; release 构建缺 checksum 失败 | `HIGH` | `P1` |
| P1-08 | `build-logic/convention/src/main/kotlin/org/autojs/build/LibDeployer.kt:338`, `build-logic/convention/src/main/kotlin/org/autojs/build/SevenZExtractor.kt:69` | zip/7z 解压缺 canonical 边界检查 | 恶意压缩包可通过 `../` 或绝对路径写出目标目录 | 写入前校验 canonical path 必须位于 `tempOut`; 拒绝绝对路径和 `..` | `HIGH` | `P1` |
| P1-09 | `.github/workflows/android.yml:26` | CI 下载 CMake installer 后 `sudo` 执行, 无 checksum/GPG | 构建 runner 直接执行远端脚本, 供应链面过大 | 改用可信 action/apt/预装工具; 必须下载时固定 SHA-256/GPG | `HIGH` | `P1` |
| P1-10 | `modules/apk-signer/src/main/java/com/mcal/apksigner/CertCreator.kt:35` | 新证书默认 `SHA1withRSA` | 弱哈希带来兼容和安全策略风险 | 默认 `SHA256withRSA` 或更强; SHA1 仅保留兼容开关 | `HIGH` | `P1` |
| P1-11 | `libs/rapidocr/src/main/cpp/src/BitmapUtils.cpp:15`, `libs/rapidocr/src/main/cpp/src/BitmapUtils.cpp:42` | JNI lock 失败前 catch 仍可能 unlock | `AndroidBitmap_lockPixels` 失败或断言异常时仍调用 unlock | 用 `locked` 布尔或 RAII; 仅锁定成功后 unlock | `MEDIUM` | `P1` |
| P1-12 | `libs/rapidocr/src/main/cpp/src/OcrResultUtils.cpp:64`, `libs/rapidocr/src/main/cpp/src/OcrResultUtils.cpp:101` | JNI local refs 不释放 | 大量文本框时 local reference table 可能溢出 | 循环内 `DeleteLocalRef`, 检查 `New*` 返回 | `MEDIUM` | `P1` |
| P1-13 | `modules/apk-parser/src/main/java/net/dongliu/apk/parser/AbstractApkFile.java:421` | APK Signing Block 偏移边界检查不足 | 畸形 APK 可触发越界/DoS | 每次 position/slice 前校验 `cdStart/blockSize/remaining` | `MEDIUM` | `P1` |
| P1-14 | `modules/apk-parser/src/main/java/net/dongliu/apk/parser/utils/Inputs.kt:8`, `modules/apk-parser/src/main/java/net/dongliu/apk/parser/ApkFile.java:66` | ZIP entry 全量读入且无限额 | 恶意 APK 大 entry/zip bomb 可导致 OOM | entry size/总解压量加上限; 尽量流式解析 | `MEDIUM` | `P1` |
