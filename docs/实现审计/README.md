# AutoJs6 实现层审计文档体系

> 日期: 2026-06-08
> 范围: 审计输入快照中的已跟踪文件、审计开始时已存在的未跟踪文档产物、以及被 `.gitignore` 忽略但与发布安全直接相关的 `sign.properties`
> 目标: 梳理 AutoJs6 底层实现结构, 标注实现层问题、原因、风险、修复办法、严重程度和优先级

## 文档索引

| 文档 | 内容 |
| --- | --- |
| [00-审计范围与方法.md](00-审计范围与方法.md) | 文件覆盖口径、分区策略、严重程度/优先级定义、覆盖统计 |
| [01-底层实现地图.md](01-底层实现地图.md) | 当前项目底层模块、运行链路、权限/插件/native/构建边界 |
| [02-P0-P1优先问题清单.md](02-P0-P1优先问题清单.md) | 需要优先处理的阻断级和高风险问题 |
| [03-运行时与脚本执行审计.md](03-运行时与脚本执行审计.md) | Rhino/脚本执行/调度/外部入口/WebView/事件循环问题 |
| [04-Core底层能力审计.md](04-Core底层能力审计.md) | Accessibility、截图、Root/Input、OpenCV、插件中心、looper 等问题 |
| [05-Native与第三方模块审计.md](05-Native与第三方模块审计.md) | RapidOCR、Paddle OCR、apk-parser、apk-signer、imagequant、第三方模块问题 |
| [06-构建发布与供应链审计.md](06-构建发布与供应链审计.md) | Gradle、CI、远程依赖、签名、wrapper、发布完整性问题 |
| [07-文件覆盖清单.md](07-文件覆盖清单.md) | 当前审计纳入的全量文件清单, 用于证明每个文件已被确认归档 |

## 关键结论

当前项目的底层能力非常宽: 脚本运行时、无障碍自动化、截图/OCR/OpenCV、Root/Shizuku/input、插件中心、APK 构建和多模块 Gradle 构建都已经成型。主要风险不是单点 API 缺失, 而是高权限执行入口、native 资源生命周期、外部供应链校验、脚本/服务生命周期竞态和二进制依赖可追溯性。

最优先处理的 P0/P1 问题集中在:

- 外部导出的脚本执行入口可被任意应用触发。
- WebView/Rhino bridge 与 file URL universal access 组合形成高风险执行面。
- 本地 `sign.properties` 存在明文签名口令。
- RapidOCR/Paddle OCR native 初始化与释放存在崩溃或竞态风险。
- 远程构建依赖下载、CI installer、压缩包解压和 Gradle wrapper 缺少完整性约束。

## 文件覆盖摘要

本轮纳入 `4945` 个文件。覆盖清单来自审计输入快照, 不包含 `.gradle/`、`build/`、崩溃日志、replay 日志等本地产物, 也不把本次新建的 `docs/实现审计` 文档产物反向计入被审输入。资源、图片、AAR/JAR/SO 等不可直接源码审计的文件已纳入清单并按“二进制/资源文件”口径确认; 对这些文件的深度结论以调用链、构建入口、版本和供应链风险为主。

## 输出口径

每个问题均按以下字段记录:

- 文件/行号
- 存在的问题
- 原因/风险
- 解决办法
- 严重程度: `CRITICAL` / `HIGH` / `MEDIUM` / `LOW`
- 优先级: `P0` / `P1` / `P2` / `P3`
